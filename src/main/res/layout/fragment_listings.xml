<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/listings_padding"
    android:paddingRight="@dimen/listings_padding">

    <!--TextView
        android:id="@+id/listings_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="Listings"
        android:visibility="visible"
        android:textSize="14sp"
        style="?android:listSeparatorTextViewStyle"  /-->

    <ListView
        android:id="@+id/listings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:visibility="visible" />

    <ProgressBar
        android:id="@+id/listings_loading_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/loading_spinner"
        android:visibility="gone" />

</RelativeLayout>